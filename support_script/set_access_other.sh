#!/bin/bash

# other: ...
chown root:root -R /etc/network
chown root:root -R /etc/apache2
chown root:root -R /etc/ssh
chown root:root -R /etc/cron.d
chown root:root -R /etc/samba
chown root:root -R /etc/cron.hourly


